# === 必須 (Phase 1) ===
pyserial>=3.5
PyYAML>=6.0

# === Spreadsheet連携 (Phase 1: google_sheets.enabled: true で必要) ===
gspread>=5.0
google-auth>=2.0

# === Discord 通知 (Phase 2: discord_webhook_url 設定時に必要) ===
requests>=2.28

# === グラフ生成 (Phase 3: センサー推移の可視化) ===
matplotlib>=3.5
pandas>=1.4

# === 将来用 ===
# opencv-python       # USBカメラ (Phase 3+)
# schedule            # 高機能スケジューラ (必要に応じて)
